mlx>=0.31.2
# Pinned: 5.10.0+ reintroduced an unconditional `import torch` in the Gemma4
# image processor, which breaks the torch-free MLX runtime. 5.9.0 guards it
# behind is_vision_available() and falls back to the PIL processor.
transformers==5.9.0
datasets>=2.19.1
miniaudio>=1.59
tqdm>=4.66.2
Pillow>=10.3.0
requests>=2.31.0
llguidance>=1.7.0
mlx-lm>=0.31.3
mlx-audio>=0.4.3
opencv-python>=4.12.0.88
fastapi>=0.95.1
python-multipart>=0.0.9
starlette>=1.0.1
uvicorn
numpy
